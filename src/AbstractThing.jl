"""
# AbstractThing
The `AbstractThing` struct is placed at the top of the taxonomy.
It makes it possible to define pseudo-universal functions to facilitate the manipulation of objects in general.
"""
abstract type AbstractThing end;
